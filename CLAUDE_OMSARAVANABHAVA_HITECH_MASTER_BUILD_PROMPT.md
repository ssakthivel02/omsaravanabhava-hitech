# CLAUDE MASTER BUILD BRIEF — OmSaravanaBhava Hi-Tech Flagship

## Mission
Build a world-class, premium, unmistakably Murugan devotional knowledge platform in the NEW clean GitHub repository `ssakthivel02/omsaravanabhava-hitech`.

This is a clean flagship migration. The primary purpose of the new repository is to permanently eliminate the recurring failure mode where a new hi-tech website later reverts to, rebuilds from, or becomes mixed with an older website implementation.

Do not restart devotional knowledge from memory. Recover and reconcile the approved native R6 source/content authority, but do not import the old website application shell, UI, CSS, JavaScript, layouts, build workflow or donor-project code.

## 1. Absolute repository authority

### NEW FLAGSHIP REPOSITORY — ONLY NEW-CODE AUTHORITY
`https://github.com/ssakthivel02/omsaravanabhava-hitech`

This repository is the only source of truth for the new hi-tech website.

Rules:
- Build all new application source here.
- Do not copy Git history from the old repository.
- Do not use a submodule pointing to the old repository.
- Do not configure a workflow that checks out the old repository during build or deployment.
- Do not generate the new site by applying a theme/overlay/rename to an old site.
- Do not deploy any artifact whose build requires the old repository.
- Do not silently fall back to older code when a file is missing.
- Do not import `node_modules`, build output, caches, local environment files, credentials, keys or temporary files.

### LEGACY / ROLLBACK REPOSITORY — REFERENCE ONLY
`https://github.com/ssakthivel02/OmSaravanaBhava`

The legacy repository may be consulted only for:
- rollback evidence;
- comparison of existing public capabilities;
- source/provenance clues;
- canonical content reconciliation when a record is independently verified.

It MUST NOT be used as:
- application source donor;
- CSS/JS donor;
- component/layout donor;
- build donor;
- deployment donor;
- visual design donor;
- runtime dependency.

Do not copy its old application shell into the new repository.

## 2. Strict project isolation
Never mix source, components, UI, styles, assets or build systems from KirthiVerse, RamaVerse, DivyaNexus, Saravana Bhava Android, OSB Training Academy, SakthiAI/SaravanAI, Kandan or any other project.

A source reference to another project in technical/runtime code is a release blocker unless it is clearly part of an isolated historical documentation note.

## 3. Current production — preserve until cutover
Production domain:
`https://omsaravanabhava.org/`

Current production remains live during the entire new build. Do not modify root-domain DNS, production routes, the current hosting target or production service-worker behavior during development.

Current public capability baseline observed on 4 September 2026 includes approximately:
- 12 canonical full-song records;
- 24 governed temple directory records;
- 16 governed read-aloud records;
- 113 governed public routes;
- Tamil-first navigation with English support;
- song library and Thiruppugazh;
- temple / Arupadai Veedu discovery;
- audio/read-aloud;
- source-aware search/guidance;
- Daily Darshan / devotional practice paths;
- mantra practice;
- PWA/offline concepts;
- accessibility and reduced-motion concepts.

These are parity/reference signals, NOT permission to copy old implementation files. The new R6 native build may redesign routes and architecture while preserving or improving legitimate governed capability.

## 4. Authoritative Manus R6 source
Preferred native source archive:
`OmSaravanaBhava_R6_FINAL_MANUS_MASTER_SOURCE.zip`

Expected SHA-256:
`3477dd375e9545bd51482f9cacabe851adc6a841cdf111ffd5eb408f76c26585`

Alternative portfolio export if the dedicated archive is unavailable:
`MANUS_ALL_WEBSITES_LATEST_EXPORT_2026-08-27.zip`

Expected SHA-256:
`47eef4cb07a6d466ad370d3923ec972ae43a916b18081ed85182d2b32da3ddfc`

When the portfolio export is used, extract ONLY:
`01_OmSaravanaBhava/source/R6_FINAL_MASTER/`

Do not extract/import any other project from the portfolio archive.

Known recovered R6 characteristics:
- native editable React/Vite application lineage;
- Express/server entry may be present in the source;
- pnpm workflow;
- canonical build contract: `pnpm run build`;
- historical recovery evidence recorded 2,521 referenced assets present and 52 unresolved asset references.

### Hash rule
Before import, calculate SHA-256 yourself and compare with the expected hash. If it differs, STOP SOURCE IMPORT and report the mismatch. Never relabel a different file as the authoritative R6 package.

If neither authoritative archive is available, do not rebuild R6 from memory and do not substitute R5 or the legacy public site. Build only scaffolding/quality infrastructure until the authoritative source is supplied.

## 5. Canonical devotional-content safety
Existing verified/canonical Murugan material must remain authoritative.

Never silently rewrite canonical devotional source text.

For every governed devotional record preserve where available:
- stable record ID;
- exact canonical Tamil text;
- work/author attribution;
- source reference;
- verification state;
- textual-variant state;
- publication/rights state;
- transliteration as a separate layer;
- easy-reading Tamil as a separate layer;
- simple meaning/commentary as a separate layer;
- audio/read-aloud provenance;
- responsible non-guarantee notes where relevant.

Never fabricate scripture, mantra wording, temple history, inscriptions, festival dates, timings, miracles, citations or historical claims.

Generated reflections must be clearly identified as generated/editorial reflection and must never masquerade as scripture, canonical text, prophecy, personalised divine instruction or religious authority.

For modern copyrighted devotional songs, do not publish full lyrics/audio unless rights permit. Metadata-only records are acceptable when publication rights are not established.

AI-generated or illustrative temple/Murugan imagery must be labelled as devotional illustration where users could otherwise mistake it for documentary/official imagery.

## 6. Product vision — Sacred-Tech, not generic SaaS
Create a distinctive premium `Sacred-Tech` experience: devotional, calm, cinematic, highly responsive and technologically advanced without becoming flashy, gamified or disrespectful.

### Visual identity
Create an unmistakable OmSaravanaBhava / Murugan identity with:
- Vel-centric geometry and symbolism;
- Murugan / Skanda / Subramanya devotional identity;
- temple architecture and pilgrimage cues;
- peacock / sacred-light motifs used sparingly and elegantly;
- premium Tamil typography;
- luminous gold/copper/saffron accents over deep devotional neutrals;
- generous readable surfaces for canonical Tamil;
- subtle depth, light, parallax and motion where useful;
- first-class reduced-motion alternative.

Avoid:
- generic dashboard appearance;
- copied SaaS landing-page templates;
- neon/casino aesthetics;
- excessive glassmorphism;
- endless identical cards;
- particle effects that harm performance/readability;
- stock-looking AI visuals everywhere;
- any visual grammar copied from another user project.

The homepage should feel like entering a Murugan devotional knowledge universe, not a software admin portal.

## 7. Core experience architecture
Build a coherent information architecture around these major experiences. Exact route names may be improved based on R6, but every journey must be intentional and testable.

### A. Flagship Home
- premium hero / sacred opening experience;
- clear OmSaravanaBhava identity;
- Tamil-first hierarchy;
- immediate entry to Songs, Thiruppugazh, Temples, Mantras/Prayers, Audio, Search/Guide and Daily Practice;
- fast initial load; hero must not destroy LCP;
- meaningful CTAs and deep links, not decorative buttons.

### B. Murugan Knowledge & Discovery
- Murugan names/forms/titles and source-aware knowledge;
- structured deity/knowledge exploration;
- source and verification labels;
- related works, temples and devotional paths.

### C. Song Library
- Collection -> Work -> Song -> Reading layers;
- canonical Tamil first;
- easy Tamil, transliteration and meaning separated;
- source/verification/publication state visible;
- search/filter/save/continue-reading where useful;
- audio state and provenance clear.

### D. Thiruppugazh
- premium Tamil reading;
- source-controlled records;
- search/browse;
- transliteration/meaning only when governed;
- audio/read-aloud clearly differentiated.

### E. Mantras / Slokas / Prayers / Namavali
- exact canonical source layer;
- meaning and transliteration separated;
- practice controls must be private/local where possible;
- no guaranteed-remedy claims;
- responsible devotional context.

### F. Temples & Pilgrimage
- Arupadai Veedu as a flagship visual journey;
- broader Murugan temple directory as governed data grows;
- temple detail pages with provenance/status;
- map/location only from verified location data;
- documentary facts separated from devotional/traditional narratives;
- official/legal direct-support links only when verified; the platform must not intermediate donations.

### G. Audio / Read-Aloud
- owned/licensed audio where rights permit;
- browser/device read-aloud labelled differently from recorded devotional audio;
- persistent but non-intrusive player;
- keyboard accessible controls;
- no autoplay with sound;
- efficient loading and memory behavior.

### H. Search / Source-Aware Guide
- fast Tamil + English search across governed content;
- source and verification state visible in results;
- zero-result state must never invent content;
- generated devotional reflection, if included, must be explicitly separated from canonical/source results;
- no prophecy or personalised divine claims;
- production must not depend on a manually available third-party AI subscription.

### I. Daily Devotion / Practice
- Daily Darshan / reflection paths;
- mantra practice counter where appropriate;
- saved/recent/library behavior using privacy-respecting browser-local storage by default;
- no coercive streaks or manipulative engagement mechanics.

### J. Trust / Sources / Legal / Help
Include clear routes for:
- About;
- Sources / source methodology;
- Content completeness / verification states;
- Disclaimer;
- Privacy;
- Terms;
- Contact/support;
- Corrections;
- Accessibility;
- Offline/PWA help;
- data/local-storage explanation where applicable.

## 8. Internationalisation
Tamil is PRIMARY.
English is strong secondary support.

Historical candidate languages may include Hindi, Telugu, Malayalam and Arabic, but do not claim a language as supported until major routes, persistence, typography, mixed-script behavior, metadata and QA pass.

Arabic additionally requires true RTL layout qualification. Do not merely translate strings while leaving an LTR interface broken.

Do not add Kannada or other languages simply for a marketing number; add languages only with real content/QA coverage.

## 9. Recommended engineering baseline
Use a modern, maintainable, dependency-conscious frontend architecture. Prefer latest stable versions available at build time rather than outdated pinned examples.

Expected direction unless R6 itself proves a better compatible structure:
- React + TypeScript;
- Vite;
- pnpm with committed lockfile;
- strict TypeScript;
- modern routing with deep-link support;
- content/data domain separated from presentation;
- semantic HTML first;
- CSS design tokens / maintainable styling rather than a giant unstructured stylesheet;
- reusable accessible primitives;
- local-first search/index where practical;
- PWA only if it is robust and testable;
- Vitest or equivalent for unit/component tests;
- Playwright for critical browser journeys;
- automated accessibility checks plus manual keyboard review;
- deterministic build.

Do not add large libraries purely for visual novelty.

Suggested source boundaries:
- `src/app/` — application shell/router/providers
- `src/components/` — shared accessible primitives
- `src/features/` — songs, temples, search, audio, practice, etc.
- `src/content/` or `src/data/` — governed content registries/loaders
- `src/lib/` — narrowly scoped utilities
- `src/styles/` — tokens/global foundations
- `public/` — governed static assets only
- `tests/` — browser/integration tests
- `docs/` — architecture/source/release evidence

Adapt to native R6 rather than forcing this exact tree if R6 already has a sound structure.

## 10. Asset governance
Historical evidence recorded 2,521 references present and 52 unresolved references. Recompute from the actual recovered R6 source.

For every unresolved reference record one disposition:
- `FOUND_VERIFIED`
- `RIGHTS_SAFE_REPLACEMENT`
- `QUARANTINED`
- `INTENTIONALLY_REMOVED`

Do not silently replace a missing image with an unrelated asset.
Do not copy imagery from other projects.
Do not ship broken local asset references.
Do not ship unlicensed media.

For each public asset maintain sufficient provenance to explain source/ownership/licence/publication status.

## 11. Performance quality bar
Target a genuinely fast premium experience.

Release targets:
- LCP <= 2.5 s at the 75th percentile target;
- INP <= 200 ms target;
- CLS <= 0.10 target;
- mobile Lighthouse Performance >= 90 on agreed critical-route lab runs;
- desktop Lighthouse Performance >= 95 on agreed critical-route lab runs;
- Accessibility >= 95;
- Best Practices >= 95;
- SEO >= 95;
- no large unoptimised hero media on initial load;
- responsive images with explicit dimensions;
- modern image formats where practical;
- lazy-load non-critical visual/audio media;
- route/code splitting where it materially helps;
- preload only true critical resources;
- Tamil fonts must be subset/cached/loaded without excessive blocking when legally permitted.

Do not game Lighthouse by disabling real functionality.

## 12. Accessibility
WCAG-oriented quality is mandatory, not optional.

Require:
- correct landmarks;
- logical heading hierarchy;
- keyboard-only operation;
- visible focus;
- accessible menus/dialogs/audio controls;
- no keyboard traps;
- meaningful alt text for informative imagery;
- decorative imagery hidden appropriately;
- sufficient contrast;
- zoom/reflow robustness;
- large enough touch targets;
- reduced-motion support;
- Tamil/English language attributes where applicable;
- RTL correctness for Arabic if enabled.

## 13. SEO / discoverability
Build production-grade technical SEO without keyword stuffing.

Require:
- correct canonical domain `https://omsaravanabhava.org/` only in production build/config;
- route-specific titles/descriptions;
- OpenGraph/social metadata;
- structured data only where truthful;
- robots.txt;
- sitemap generated from PUBLIC routes only;
- hreflang only for genuinely supported language variants;
- clean semantic URLs;
- no indexable preview/staging duplicates;
- source-aware content structure useful to both users and search engines.

## 14. Security / privacy
- no secrets/tokens in browser code or repository;
- no committed `.env` secrets;
- no credentials in GitHub Actions;
- use GitHub/Cloudflare secrets only when owner configures them;
- no wildcard CORS without a justified public-resource requirement;
- no sensitive personal data in static content;
- browser-local personal practice data by default where appropriate;
- external analytics only if privacy/legal configuration is explicit;
- dependency audit and secret scan in CI.

## 15. Git workflow
This new repository must remain clean and auditable.

Recommended workflow:
1. Bootstrap `main` with project authority/docs/CI only.
2. Create `build/native-r6-flagship` (or equivalent) for native application import/build.
3. Import only hash-verified R6 source.
4. Commit in logical batches.
5. Open a draft PR to `main`.
6. Keep the PR draft while release gates remain incomplete.
7. Do not merge because a basic build alone is green.
8. Do not force push or rewrite public history.
9. After bootstrap, prefer PR-based changes and branch protection/rules where owner permissions support them.

Never import old repository history into this new repo.

## 16. CI gates
Create GitHub Actions that fail closed.

At minimum:
1. repository/source provenance validation;
2. dependency install with frozen lockfile;
3. TypeScript typecheck;
4. lint/static analysis;
5. unit/component tests where present;
6. `pnpm run build`;
7. route/deep-refresh tests;
8. broken local asset scan;
9. secret/private-data scan;
10. cross-project contamination scan;
11. critical Playwright journeys;
12. accessibility checks;
13. reduced-motion checks;
14. PWA/offline checks if PWA is supported;
15. metadata/robots/sitemap/hreflang checks;
16. performance budget checks;
17. exact build SHA/artifact evidence.

A missing mandatory source or test must not be converted into a fake PASS.

## 17. Cloudflare / DNS / deployment architecture
Domain and DNS are managed through Cloudflare.

### Development / preview
DO NOT point `omsaravanabhava.org` to this new project during development.

Use one of these safe preview models depending on available authenticated Cloudflare tooling:
- a unique `*.workers.dev` preview deployment; OR
- an isolated preview custom hostname such as `hitech-preview.omsaravanabhava.org` only after owner approval; OR
- a GitHub Actions build artifact when Cloudflare credentials are not available.

Preferred Cloudflare project/worker naming should be unmistakable, e.g.:
- `omsaravanabhava-hitech-preview`
- later production project `omsaravanabhava-hitech`

Do not reuse an old worker/project name if doing so could serve stale assets or old code.

### Production cutover
Production cutover is a separate explicit gate.

Claude MUST NOT change root DNS/custom-domain routing merely because the new build succeeds.

Only after the owner explicitly writes a production approval equivalent to `DEPLOY PRODUCTION` should the release process:
1. record exact Git commit SHA;
2. build that exact commit from the NEW repo only;
3. verify release artifact identity;
4. verify critical routes/mobile/a11y/performance/PWA/SEO;
5. verify source/rights gates;
6. preserve the old production target as rollback evidence;
7. attach/switch `omsaravanabhava.org` to the new Cloudflare deployment using supported Cloudflare mechanisms;
8. verify HTTPS/canonical redirects/live routes;
9. validate that no stale service worker or old asset cache can make the new domain appear to revert;
10. keep rollback instructions documented.

Do not delete the old repo as part of cutover. Archive/label it later only after the new production has proven stable.

## 18. Anti-reversion controls — CRITICAL
The previous migration failure pattern is specifically that a new hi-tech site later shows the old website again. Prevent this by design.

Mandatory anti-reversion controls:
- new repo has no old website source;
- CI must never checkout old repo;
- deployment workflow must build only `ssakthivel02/omsaravanabhava-hitech` at the exact Git SHA;
- Cloudflare project/worker must have a new unique name;
- preview/prod artifact must contain a machine-readable release marker with repo, commit SHA, build timestamp/version and source authority;
- expose a harmless `/release.json` or equivalent diagnostic artifact in deployments;
- release marker must include `repository: ssakthivel02/omsaravanabhava-hitech`;
- release verification must fail if legacy repo/project identifiers are present in runtime bundle where they should not be;
- service-worker cache namespace/version must be unique to the hi-tech application;
- old caches must not be reused;
- service-worker update strategy must be tested;
- validate the live domain in a fresh/private browser and after service-worker unregister/cache clear during cutover QA;
- verify HTML and key JS asset hashes after deployment;
- never deploy a directory produced by the old build workflow;
- never use a generic `--assets=.` from repository root unless the root is intentionally the validated production artifact directory; deploy the explicit build output directory instead;
- preserve a release manifest and rollback manifest for every production cutover.

## 19. Release evidence matrix
Before production, provide a concise machine-readable + human-readable report covering at least:
- exact source archive/hash;
- exact Git SHA;
- build PASS;
- critical routes PASS;
- home desktop/mobile PASS;
- songs PASS;
- Thiruppugazh PASS;
- mantras/prayers PASS;
- Arupadai Veedu/temples PASS;
- search PASS;
- audio/read-aloud PASS;
- Tamil canonical rendering PASS;
- Tamil UI PASS;
- English UI PASS;
- any other claimed languages PASS;
- keyboard/focus PASS;
- reduced motion PASS;
- PWA/offline PASS or explicitly not supported;
- SEO/canonical/sitemap PASS;
- security/secret scan PASS;
- asset rights/provenance PASS;
- performance budget PASS;
- no cross-project contamination PASS;
- anti-reversion release-marker check PASS;
- rollback evidence READY.

If any P0 item is not proven, report NO-GO instead of `production ready`.

## 20. Execution phases
Do not spend most of the effort writing audit prose. Execute the build.

### Phase 0 — Clean-room recovery
- verify new repo state;
- ingest this master brief;
- hash-verify supplied R6 archive;
- extract/stage only OmSaravanaBhava R6 source;
- inventory source, routes, assets, dependencies and content registries;
- identify unresolved references;
- create architecture/recovery report.

### Phase 1 — Native application foundation
- import native R6 cleanly;
- normalize dependency/build setup without changing devotional content;
- establish TypeScript/build/test baseline;
- create design tokens and accessibility foundations;
- establish routing and release marker.

### Phase 2 — Premium flagship shell
- build/refine homepage, navigation, global search entry and sacred visual system;
- make desktop/tablet/mobile first-class;
- implement reduced-motion parity;
- avoid generic dashboard/card repetition.

### Phase 3 — Core devotional journeys
- Songs;
- Thiruppugazh;
- Mantras/Prayers;
- Temples/Arupadai Veedu;
- Audio/read-aloud;
- Source-aware search;
- Daily devotion/practice;
- Trust/source routes.

### Phase 4 — Content reconciliation
- compare R6 records with verified canonical evidence;
- resolve all asset references;
- preserve exact Tamil;
- quarantine unverified/restricted content;
- ensure all public records have truthful status.

### Phase 5 — Quality engineering
- browser E2E;
- responsive visual QA;
- accessibility;
- multilingual qualification;
- PWA/offline;
- performance;
- security;
- SEO;
- release evidence.

### Phase 6 — Preview release
- deploy isolated preview from NEW repo only;
- publish exact release marker;
- run fresh-browser testing;
- provide preview URL + commit SHA + evidence report;
- do NOT alter production DNS.

### Phase 7 — Production cutover
Only after explicit owner production approval.

## 21. Required first response from Claude
Do not merely say you understand. Start work immediately.

In your first substantive response provide:
1. `NEW REPO VERIFIED` — repository and current branch/head;
2. `PRODUCTION PRESERVED` — confirm no DNS/production mutation;
3. `R6 SOURCE STATUS` — which authoritative archive was supplied and its computed SHA-256, or `BLOCKED_SOURCE_BYTES`;
4. `CLEAN-ROOM CHECK` — evidence that no old repo/project source was imported;
5. `ARCHITECTURE` — concise final technical direction;
6. `BUILD STATUS` — install/typecheck/lint/test/build result if source is available;
7. `TOP DEFECTS/GAPS` — evidence-backed only;
8. `FILES/COMMITS CREATED`;
9. `NEXT EXECUTION BATCH`;
10. `PRODUCTION STATUS: NO-GO` until all release gates are proven.

Then continue executing safe, non-destructive work without repeatedly asking for approval. Ask the owner only when a genuinely owner-only action is required (credentials, rights decision, production DNS/cutover approval, or inaccessible source archive).

## 22. Definition of success
The work is successful only when:
- the new site is built entirely from `ssakthivel02/omsaravanabhava-hitech`;
- native R6 provenance is verified;
- canonical devotional integrity is preserved;
- the visual experience is unmistakably OmSaravanaBhava/Murugan and materially better than the legacy implementation;
- all critical controls actually work;
- mobile experience is excellent;
- accessibility/reduced motion are first-class;
- performance targets are demonstrated;
- the site cannot silently revert to an old build through CI, deployment, DNS or service-worker cache reuse;
- exact release evidence is available;
- production cutover occurs only after explicit owner approval.

Do not optimize for the phrase “top 1 website” as a marketing claim. Optimize for the controllable standard: exceptional devotional authenticity, premium design, engineering quality, speed, accessibility, trustworthy sources, excellent search/discovery, robust mobile experience and release discipline capable of competing with the best web experiences in the world.
